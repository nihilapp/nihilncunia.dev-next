# 구현 체크리스트

## 개요

필요기능도출.md의 기능들을 기준으로 그룹핑하여, 각 기능에 해당하는 페이지 라우트와 API 라우트를 하나의 세트로 묶은 체크리스트입니다.

---

## 1. 인증 시스템 (Authentication)

### 1.1 기본 인증 기능

#### 회원가입

- [x] `/auth/signup` 페이지
- [x] `POST /api/auth/signup` API

#### 로그인

- [ ] `/auth/signin` 페이지
- [ ] `POST /api/auth/signin` API

#### 로그아웃

- [ ] `POST /api/auth/signout` API

#### 비밀번호 재설정

- [ ] `/auth/forgot-password` 페이지
- [ ] `/auth/new-password` 페이지
- [ ] `/admin/profile/reset-password` 페이지
- [ ] `POST /api/auth/reset-password` API
- [ ] `PATCH /api/users/[id]/password` API

#### 이메일 인증

- [ ] `/auth/verify-email` 페이지
- [ ] `POST /api/auth/verify-email` API

#### OTP 시스템

- [ ] `POST /api/auth/otp` API
- [ ] `POST /api/auth/otp/verify` API

#### 세션 관리

- [ ] `GET /api/auth/session` API

---

## 2. 사용자 관리 (User Management)

### 2.1 프로필 관리

#### 사용자 계정 생성 (초기 설정)

- [ ] `POST /api/users` API

#### 사용자 프로필 조회/수정

- [ ] `GET /api/users/[id]` API
- [ ] `PATCH /api/users/[id]` API

#### 프로필 관리 페이지

- [ ] `/admin/profile` 페이지
- [ ] `/admin/profile/social` 페이지

---

## 3. 포스트 관리 (Post Management)

### 3.1 포스트 CRUD

#### 포스트 목록

- [ ] `/posts` 페이지
- [ ] `/admin/posts` 페이지
- [ ] `GET /api/posts` API

#### 포스트 작성

- [ ] `/admin/posts/new` 페이지
- [ ] `POST /api/posts` API

#### 포스트 상세

- [ ] `/posts/[id]` 페이지
- [ ] `/admin/posts/[id]` 페이지
- [ ] `GET /api/posts/[id]` API

#### 포스트 수정

- [ ] `/admin/posts/[id]/edit` 페이지
- [ ] `PATCH /api/posts/[id]` API

#### 포스트 삭제

- [ ] `DELETE /api/posts/[id]` API

### 3.2 포스트 검색 및 필터링

#### 포스트 검색

- [ ] `/search` 페이지
- [ ] `GET /api/posts/search` API

#### 카테고리별 포스트

- [ ] `GET /api/posts/category/[id]` API

#### 태그별 포스트

- [ ] `GET /api/posts/tag/[id]` API

#### 사용자별 포스트

- [ ] `GET /api/posts/user/[id]` API

### 3.3 아카이브 시스템

#### 아카이브 목록

- [ ] `/archive` 페이지
- [ ] `GET /api/posts/archive` API

#### 연월별 포스트

- [ ] `/archive/[id]` 페이지

---

## 4. 카테고리 관리 (Category Management)

### 4.1 카테고리 CRUD

#### 카테고리 목록

- [ ] `/categories` 페이지
- [ ] `/admin/categories` 페이지
- [ ] `GET /api/categories` API

#### 카테고리 생성

- [ ] `/admin/categories/new` 페이지
- [ ] `POST /api/categories` API

#### 카테고리 상세

- [ ] `/categories/[id]` 페이지
- [ ] `/admin/categories/[id]` 페이지

#### 카테고리 수정

- [ ] `/admin/categories/[id]/edit` 페이지
- [ ] `PATCH /api/categories/[id]` API

#### 카테고리 삭제

- [ ] `DELETE /api/categories/[id]` API

---

## 5. 태그 관리 (Tag Management)

### 5.1 태그 CRUD

#### 태그 목록

- [ ] `/tags` 페이지
- [ ] `/admin/tags` 페이지
- [ ] `GET /api/tags` API

#### 태그 생성

- [ ] `POST /api/tags` API

#### 태그 상세

- [ ] `/tags/[id]` 페이지
- [ ] `/admin/tags/[id]` 페이지

#### 태그 삭제

- [ ] `DELETE /api/tags/[id]` API

---

## 6. 시리즈 관리 (Series Management)

### 6.1 시리즈 CRUD

#### 시리즈 목록

- [ ] `/series` 페이지
- [ ] `/admin/series` 페이지
- [ ] `GET /api/series` API

#### 시리즈 생성

- [ ] `/admin/series/create` 페이지
- [ ] `POST /api/series` API

#### 시리즈 상세

- [ ] `/series/[id]` 페이지
- [ ] `/admin/series/[id]` 페이지
- [ ] `GET /api/series/[id]/posts` API

#### 시리즈 수정

- [ ] `/admin/series/[id]/edit` 페이지
- [ ] `PATCH /api/series/[id]` API

#### 시리즈 삭제

- [ ] `DELETE /api/series/[id]` API

#### 시리즈 내 포스트

- [ ] `/series/[id]/[post_id]` 페이지

---

## 7. 댓글 시스템 (Comment System)

### 7.1 댓글 관리

#### 댓글 목록

- [ ] `/admin/comments` 페이지
- [ ] `GET /api/comments` API

#### 댓글 작성

- [ ] `POST /api/comments` API

#### 댓글 상세

- [ ] `/admin/comments/[id]` 페이지

#### 댓글 수정/승인

- [ ] `PATCH /api/comments/[id]` API

#### 댓글 삭제

- [ ] `DELETE /api/comments/[id]` API

#### 댓글 검색

- [ ] `GET /api/comments/search` API

#### 포스트별 댓글

- [ ] `GET /api/comments/post/[id]` API

#### 댓글 답글

- [ ] `GET /api/comments/[id]/replies` API
- [ ] `POST /api/comments/[id]/replies` API

---

## 8. 파일 관리 (File Management)

### 8.1 이미지 업로드

#### 파일 업로드

- [ ] `POST /api/upload` API

---

## 9. 관리자 대시보드 (Admin Dashboard)

### 9.1 대시보드

#### 관리자 대시보드

- [ ] `/admin` 페이지

---

## 10. 사이트 설정 (Site Settings)

### 10.1 설정 관리

#### 사이트 설정

- [ ] `/admin/settings` 페이지

---

## 11. 휴지통 관리 (Trash Management)

### 11.1 휴지통

#### 휴지통 목록

- [ ] `/admin/trash` 페이지

#### 삭제된 포스트

- [ ] `/admin/trash/posts` 페이지

#### 삭제된 댓글

- [ ] `/admin/trash/comments` 페이지

#### 삭제된 시리즈

- [ ] `/admin/trash/series` 페이지

---

## 12. 홈페이지 (Homepage)

### 12.1 메인 페이지

#### 홈페이지

- [ ] `/` 페이지

---

## 구현 우선순위별 그룹핑

### Phase 1: 핵심 기능 (1-2주)

#### 인증 시스템

- [x] 회원가입 (페이지 + API)
- [ ] 로그인 (페이지 + API)
- [ ] 로그아웃 (API)
- [ ] 세션 관리 (API)
- [ ] 비밀번호 재설정 (페이지 + API)

#### 사용자 관리

- [ ] 사용자 계정 생성 (API)
- [ ] 프로필 조회/수정 (API)
- [ ] 프로필 관리 페이지

#### 포스트 관리

- [ ] 포스트 목록 (페이지 + API)
- [ ] 포스트 작성 (페이지 + API)
- [ ] 포스트 상세 (페이지 + API)
- [ ] 포스트 수정 (페이지 + API)
- [ ] 포스트 삭제 (API)

#### 관리자 대시보드

- [ ] 관리자 대시보드 (페이지)

### Phase 2: 확장 기능 (2-3주)

#### 카테고리 관리

- [ ] 카테고리 목록 (페이지 + API)
- [ ] 카테고리 생성 (페이지 + API)
- [ ] 카테고리 수정 (페이지 + API)
- [ ] 카테고리 삭제 (API)

#### 태그 관리

- [ ] 태그 목록 (페이지 + API)
- [ ] 태그 생성 (API)
- [ ] 태그 삭제 (API)

#### 시리즈 관리

- [ ] 시리즈 목록 (페이지 + API)
- [ ] 시리즈 생성 (페이지 + API)
- [ ] 시리즈 수정 (페이지 + API)
- [ ] 시리즈 삭제 (API)

#### 댓글 시스템

- [ ] 댓글 목록 (페이지 + API)
- [ ] 댓글 작성 (API)
- [ ] 댓글 수정/승인 (API)
- [ ] 댓글 삭제 (API)

#### 검색 시스템

- [ ] 포스트 검색 (페이지 + API)

### Phase 3: 고급 기능 (1-2주)

#### 아카이브 시스템

- [ ] 아카이브 목록 (페이지 + API)
- [ ] 연월별 포스트 (페이지)

#### 파일 관리

- [ ] 이미지 업로드 (API)

#### 휴지통 관리

- [ ] 휴지통 목록 (페이지)
- [ ] 삭제된 항목 관리 (페이지)

#### 사이트 설정

- [ ] 사이트 설정 (페이지)

### Phase 4: 마무리 (1주)

#### 홈페이지

- [ ] 홈페이지 (페이지)

#### 기타 기능

- [ ] 이메일 인증 (페이지 + API)
- [ ] OTP 시스템 (API)
- [ ] 댓글 답글 (API)
- [ ] 카테고리별/태그별/사용자별 포스트 (API)
- [ ] 시리즈 내 포스트 (페이지 + API)

---

## 완료도 추적

### 전체 완료도: 2/89 (2.2%)

### Phase 1 완료도: 1/25 (4%)

### Phase 2 완료도: 0/35 (0%)

### Phase 3 완료도: 0/20 (0%)

### Phase 4 완료도: 0/9 (0%)

---

## 참고사항

- 각 체크박스는 해당 기능의 페이지와 API가 모두 구현되었을 때 체크합니다.
- 일부 기능은 페이지만 있거나 API만 있는 경우도 있으니 주의하세요.
- 구현 순서는 Phase 1 → Phase 2 → Phase 3 → Phase 4 순서로 진행합니다.
